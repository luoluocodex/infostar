# AGENTS.md

> 本文件面向在本仓库工作的 AI 协作方（Codex / WorkBuddy / 其他 Agent）。
> 它约束「怎么改、改哪里、改完怎么交付」，不替代主方案。

## 响应规则

- **Always respond in Chinese**（全程使用简体中文）。
- 每轮交付必须给出 §5「交付输出（每次必给）」的七项，缺项视为交付不合格。
- 不确定就问，不猜着改：涉及范围、边界、技术栈、内容来源的歧义，先反馈再动手。
- 不夸大、不隐瞒：没跑过的验证不说「已验证」，跑不了的要说明原因。

---

## 0. 目标

本文件用于指导 Agent 在 `infostar` 仓库中进行自动化修改与交付，确保：

- 不偏离当前「**小学信息学十佳之星** 游戏化学习 Web App」的产品定位。
- 明确区分**主方案**、**操作型指南**、**原始资料**三者的职责与优先级。
- 不在用户未确认时提前创建代码工程、后端服务、账号体系或云同步。
- 修改可阅读、可验证、可追踪、可回滚，并服务于后续 React + Vite 移动端 Web App 落地。

本仓库**当前阶段是「纯文档阶段」**——文档体系已就位（`docs/plan` / `docs/reference` / `docs/guides`），代码工程尚未创建。默认工作重心为：

- 根目录 `README.md`
- 根目录 `AGENTS.md`（本文件）
- 根目录 `CHANGELOG.md`
- 根目录 `.gitignore`
- `docs/README.md`
- `docs/plan/小学信息学十佳之星-WebApp开发方案.md`（**主方案，唯一权威蓝图**）
- `docs/guides/*`（01 结构 · 02 部署 · 03 题库规范 · 04 MVP 规格 · 05 术语）
- `docs/reference/*`（**只读不改**）

进入代码阶段（Phase 0 起）后，默认活跃范围扩展到：

- `src/*`（`routes/` `engine/` `components/` `content/` `store/` `i18n/` `styles/`）
- `public/*`
- `package.json`、`vite.config.ts`、`tsconfig.json`、`tailwind.config.ts`
- `Dockerfile`、`docker-compose.yml`、`nginx.conf`
- 测试目录与 `e2e/*`

补充说明：

- `docs/plan/` 是**唯一权威蓝图**。其它文档与它冲突时，以它为准，并在交付中明确指出差异。
- `docs/guides/` 是**操作手册**：把主方案落成「照着能干」的步骤，不另立方案。
- `docs/reference/` 是**原始资料**（考纲 / 真题 / 训练题 / NOI 大纲），用于内容设计与判题依据，**只增不改**。
- `.workbuddy/` 是项目记忆与提取中间产物（`_ref/txt/`）；`memory/` 随仓库版本化，`_ref/` 不入库。
- 项目以**移动端浏览器优先**，体验目标接近原生 App；不要按桌面后台、传统题库站或营销落地页来设计。

---

## 1. 开始任何改动前（必做）

1. 先读根 [README.md](README.md)，确认仓库定位、当前阶段与文档导航。
2. 先读根 [AGENTS.md](AGENTS.md) 本文件，确认仓库级约束、执行方式与交付要求。
3. 先读 [docs/plan/小学信息学十佳之星-WebApp开发方案.md](docs/plan/小学信息学十佳之星-WebApp开发方案.md)，确认当前产品、内容、交互与技术主线。
4. 先读 [docs/guides/01-项目结构与目录说明.md](docs/guides/01-项目结构与目录说明.md)，确认目录职责、命名约定与「常见任务该改哪里」。
5. 若任务涉及出题、改题、内容改编、题型设计，必须读 [docs/guides/03-内容与题库规范.md](docs/guides/03-内容与题库规范.md)，再按需读取 `docs/reference/` 对应原件。
6. 若任务涉及部署、HTTPS、安全加固，必须读 [docs/guides/02-部署指南（群晖NAS）.md](docs/guides/02-部署指南（群晖NAS）.md)。
7. 若任务涉及 UI 文案或术语表述，必须读 [docs/guides/05-术语勘误与小学化对照表.md](docs/guides/05-术语勘误与小学化对照表.md)。
8. 在目标目录树内优先查找并遵守更深层的 `AGENTS.md`；**更深层规则覆盖上层规则**。当前仓库若无更深层 `AGENTS.md`，以根级为准。
9. 先判断本次改动属于哪一类：
   - 文档初始化与治理
   - 产品方案与 MVP 范围调整
   - 参考资料整理、考纲提炼与题库改编
   - React + Vite 工程初始化
   - 移动端 UI / PWA / 交互体验
   - 关卡引擎 / 题型渲染器 / 题库数据
   - C++ 子集解释器与可视化播放器
   - 拖拽积木引擎
   - 本地进度 / 存档 / 错题本 / 星级
   - 部署、HTTPS 与安全加固
   - 测试、构建与质量门禁
10. 若改动会改变技术栈、MVP 边界、内容来源使用方式、部署形态或移动端体验原则，**必须先同步更新主方案和 README，再改代码或其它文档**。
11. 若发现「README / 主方案 / 结构说明 / 题库规范 / 当前实现」之间存在差异，默认：**以主方案为蓝图，以当前实现为事实**，并在交付中明确指出差异。
12. 若用户明确要求「只做方案」「只做评审」「先确认再执行」，**不得擅自落代码或创建工程骨架**。

---

## 2. 硬性约束

### 2.0 当前主动文档入口

当前主动文档入口固定为：

- `README.md`
- `AGENTS.md`
- `docs/plan/小学信息学十佳之星-WebApp开发方案.md`
- `docs/guides/01-项目结构与目录说明.md`
- `docs/guides/03-内容与题库规范.md`

辅助 / 按需文档：

- `docs/guides/02-部署指南（群晖NAS）.md`
- `docs/guides/04-MVP-12关内容规格.md`
- `docs/guides/05-术语勘误与小学化对照表.md`
- `docs/README.md`（文档索引）
- `docs/reference/*`（原始资料，只读）
- `CHANGELOG.md`

### 2.1 八条锁定决策（不可擅自变更）

以下决策已与用户确认锁定，**未经用户明确同意不得推翻**：

| # | 决策 |
|---|---|
| 1 | **纯前端「可视化执行」**，不做后端真实 C++ 编译判题 |
| 2 | 题库按考纲**改编 / 扩充 / 原创**，但每题必须标注来源 `pastpaper` / `adapted` / `original` |
| 3 | 游戏化 = **闯关地图 + 星级**（线性）；**不做**宠物养成、排行榜、社交、多人对战 |
| 4 | **单个孩子自学**：无登录、无账号、无云同步；进度存本地，可导出备份 |
| 5 | 部署：群晖 NAS + Docker（**x86_64**），**自有域名 + Let's Encrypt**，容器只绑 `127.0.0.1:8081`，只转发 80/443，**绝不暴露 DSM 5000/5001** |
| 6 | 代码输入 = **拖拽拼积木**（自研轻量引擎，**不引入 Blockly**） |
| 7 | 技术栈：React 18 + Vite + TS(strict) + Tailwind + Zustand + Framer Motion + Dexie + Zod + vite-plugin-pwa；部署用 **nginx:alpine 单容器** |
| 8 | 界面**简体中文**，术语必须换成小学语言（见 `guides/05`） |

### 2.2 技术栈约束摘要

已确认的技术方向：

- React 18 + TypeScript（`strict: true`）+ Vite
- Tailwind CSS（设计令牌集中在 `src/styles/`，不在组件里散落魔法数值）
- Zustand（客户端状态）
- React Router（路由）
- Framer Motion（动画与页面过渡）
- Dexie / IndexedDB（进度与存档；不用裸 `localStorage` 存业务数据）
- Zod（题库 Schema 与构建期校验）
- `vite-plugin-pwa`（PWA / Service Worker）
- 纯前端静态部署，内容数据以 TypeScript 常量 / JSON 形式存在

未经用户确认，**不要引入**：

- 后端 API 服务、数据库服务
- 用户登录 / 账号体系 / 云同步
- 在线 C++ 编译器、在线判题（OJ）
- 教师后台、家长端
- 支付、排行榜、多人联机、AI 辅导
- Blockly、CodeMirror 全量编辑器等重型依赖

### 2.3 产品边界约束

本项目定位为「**信息学学习游戏**」，不是：

- 传统刷题站、在线判题系统
- 成人 IDE、代码编辑器
- 网课平台、机构 SaaS 后台

MVP 必须优先围绕以下闭环：

```text
进入应用 → 选择关卡 → 学习概念 → 动手互动 → 完成练习 → 获得反馈 → 保存进度 → 错题重练
```

**内容配比口径（v2.0 起，勿混用）**：

- 事实陈述：初试笔试权重 = 数学思考题 **70%** : 程序阅读/填空 **30%**。
- 题库题量配比：取「**数学为主线**」= 数学类（A/B/C）**240** : 程序算法类（D/E）**140** : 真题（F）**20** = **400**。
- 不要再把「70:30」当作题库题量配比使用。

### 2.4 移动端与体验约束

- 默认以手机端（竖屏）为主，不把桌面端当作第一设计对象。
- 首屏应是应用体验，不做营销式落地页。
- 交互接近原生 App：清晰导航、轻量过渡、明确反馈、适配安全区。
- 尽量使用点击、拖拽、滑动、排序、步进，不依赖大量键盘输入。
- 代码阅读区必须可读、可折叠，**不得造成页面横向溢出**。
- 主按钮点击区域不低于 **44px**。
- 每个关卡每屏只处理一个主要学习动作。
- 错误反馈必须解释原因，不能只显示「错了」。

### 2.5 内容与题库约束

- 内容边界 = **考纲**（见主方案 §1.1），不得越界引入超纲语法或算法。
- 每道题必须包含：知识点、交互类型、答案、解释、错误反馈、来源 `kind`。
- 数学题必须先拆成**可观察、可操作**的过程，再抽象为规则。
- 程序题必须展示**变量变化、执行路径或输出变化**。
- 每道题强制绑定一个生活化类比，按「**场景 → 动手 → 抽象 → 回到 C++**」四步讲透。
- 每个核心知识点至少设计**入门 / 标准 / 挑战**三个层次。
- `docs/reference/` 中的考纲与真题原卷**仅供内部研读与改编，不得原样照录进产品**。

**两条工程约定（写代码 / 出题必须遵守）**：

- **约定 G1（cin）**：`cin` 取值**只从题目预置的 `payload.testInput` 读取，不弹输入框**；`code-*` 题的 payload 必含 `code` / `testInput` / `expectedStdout`。
- **约定 G2（语法白名单）**：题库代码只允许使用引擎已实现的语法子集；`code-*` 题在构建期做白名单校验。MVP 积木**只做 L1 填空式**（槽位限数字 / 运算符 / 下标 / 变量名）。

**术语勘误（勿照抄考纲笔误）**：

- `cin` 是**输入**语句（考纲原文误写「输出语句」）。
- 排序算法统一写「**选择排序**」（考纲原文写「选大排序」）。
- 改错题里的 `#inlude` / `iostram` / `maln()` 等是**刻意错误**，作为「找 Bug」素材保留，不要「修好」原卷文本。

### 2.6 工程分层约束

创建代码工程时，优先遵守以下边界（与 `guides/01` §3 一致）：

- `src/routes/`：路由级页面，只做编排，不承载复杂业务逻辑。
- `src/engine/cpp/`：C++ 子集解释器（lexer / parser / ast / interpreter / types / errors）。
- `src/engine/blocks/`：拖拽积木数据模型、槽位定义与静态校验。
- `src/components/viz/`：可视化组件（变量盒子 / 数组柜 / 矩阵 / 打字机 / 循环轮盘 / 条件岔路 / 栈 / 队列）。
- `src/components/runner/`：Trace 播放器与代码高亮。
- `src/content/`：题库数据（按区域拆包，动态 import）+ Zod Schema。
- `src/store/`：进度、设置、Dexie 库定义。
- `src/i18n/zh.ts`：全部中文文案集中管理。
- `src/styles/`：设计令牌与动画。

**分层铁律**：

1. `engine/` **不 import UI**，纯函数纯数据，必须可在 Node 下单测。
2. `content/` **不 import `components/`**，题库只是数据。
3. 文案**不硬编码在组件里**，一律走 `i18n/zh.ts`。
4. 可视化组件**只读 `Frame`**，不自己算程序逻辑。

**避免**：

- 每个关卡硬编码一个页面。
- 题目数据直接写进页面组件。
- 程序阅读题写死在播放器组件里。
- 进度、存档、星级分别实现多套存储逻辑。
- 把所有业务逻辑堆进 `components/`。

### 2.7 文档与 README 约束

- 新增重要目录时，应补充 README 或在 `guides/01` 中说明职责。
- 改动涉及范围、边界、技术栈、内容来源、MVP 计划、部署方式时，必须同步更新相关文档。
- 文档应面向开发者，优先讲清**职责、边界、下一步、验证方式**。
- 中文文档使用 **UTF-8**。
- Markdown 链接尽量使用**相对路径**，方便仓库内阅读。
- `README.md` 保持轻量，作为入口与导航，不承载过长方案正文。
- 主方案升级时，必须在 `docs/plan/` 主方案 §0 版本历史与 `CHANGELOG.md` **同步留痕**。

### 2.8 部署与安全约束

- 目标架构：`React build → dist → nginx:alpine 容器 → 群晖反向代理 → 自有域名（HTTPS）`。
- 容器端口映射固定 `127.0.0.1:8081:80`，**不得绑 `0.0.0.0`**。
- **绝不**在容器或文档中暴露 DSM `5000/5001`。
- 公网暴露必须满足主方案 §9.4 安全清单（安全响应头、访问口令、限流、最小暴露面）。
- Access 口令（HTTP Basic Auth）推荐落在**容器 nginx 层**（预生成 `.htpasswd` 打进镜像），不依赖群晖层。
- Service Worker 仅在 `https:` 下注册；HTTP 入口静默降级为在线模式（渐进增强，不报错、不阻断）。
- 不得为了 MVP 引入不必要的服务端运行时。

### 2.9 Git 与提交约束

- 提交信息使用中文，并带类型前缀：`docs:` / `feat:` / `fix:` / `refactor:` / `test:` / `build:` / `chore:`。
- 一次提交只做一件事；文档改动与代码改动尽量分开提交。
- 不提交 `node_modules/`、`dist/`、`coverage/`、`.env`、`.htpasswd` 等（见 `.gitignore`）。
- **不擅自 commit / push**：除非用户明确要求，否则完成改动后给出建议的 commit message，由用户决定。
- `docs/reference/` 含考纲与真题原卷，是否入库涉及版权，**按用户决定处理，默认不动**。

---

## 3. 执行 Playbook

### 3.1 文档治理

- 先确认当前主动文档入口，再修改具体内容。
- 主方案变更优先落到 `docs/plan/小学信息学十佳之星-WebApp开发方案.md`。
- 目录职责、工程结构、命名约定变更优先落到 `docs/guides/01-项目结构与目录说明.md`。
- 资料来源、题型分析、内容改编规则变更优先落到 `docs/guides/03-内容与题库规范.md`。
- 部署步骤与安全清单变更优先落到 `docs/guides/02-部署指南（群晖NAS）.md`。
- UI 文案与术语口径变更优先落到 `docs/guides/05-术语勘误与小学化对照表.md`。
- `docs/reference/` **只读不改**；若资料有误，改的是 `guides/05` 的勘误表，不是原卷。

### 3.2 产品方案与 MVP 调整

- 任何新增功能都要判断是否属于 MVP。
- 若属于 MVP，说明它服务核心闭环的哪一环。
- 若不属于 MVP，默认放入后续阶段，不抢占当前主线。
- 不用题量掩盖交互质量；MVP 优先验证「学习过程是否更容易理解」。
- MVP 定义（不可缩水）：内容只做 **12 关**（C 区「爬楼梯的秘密」6 关 + D 区「会变的盒子」6 关），但**闭合回路必须完整**——地图、关卡五段式流程、3 种题型、拖拽积木 v1、可视化执行引擎 v1、星级、存档、PWA、Docker 部署，一个不能少。

### 3.3 参考资料与内容改编

- 先从 `guides/03` 获取规范，再按需打开 `docs/reference/` 原件。
- 高阶材料（NOIP / NOI 大纲）**只作为长期路线**；进入主线时必须拆成低阶体验。
- C++ 旧版训练题可用于程序阅读题型提炼与改错题校对。
- 改编题目必须写清：知识点、场景、交互动作、答案、解释、来源标注。
- 改编后必须能通过 `npm run validate:content`（代码阶段）。

### 3.4 React + Vite 工程初始化

**只有在用户明确要求进入代码阶段时才初始化工程。**

推荐顺序：

1. React + TypeScript + Vite 脚手架（严格模式）。
2. 移动端 App Shell 优先（安全区、竖屏、路由骨架）。
3. 先做首页 / 地图页 / 关卡页**空状态**。
4. 再接入内容数据模型与 Zod Schema。
5. 最后实现核心互动组件与可视化引擎。

初始化后必须同步更新：

- `README.md`（快速开始、命令）
- `docs/guides/01-项目结构与目录说明.md`
- `CHANGELOG.md`
- 必要时更新主方案

### 3.5 交互组件与可视化开发

优先级：

1. C++ 可视化执行引擎（Frame Trace 生成）
2. Trace 播放器
3. 变量盒子
4. 条件岔路
5. 循环轮盘
6. 拖拽积木与放置槽
7. 数组柜 / 矩阵
8. 排序小火车
9. 星级结算与本地进度 / 错题本

实现时保持**内容数据与渲染组件分离**。

### 3.6 部署与 PWA

MVP 部署方向为纯前端静态站点：

```text
React build → dist → nginx:alpine（群晖 Docker）→ 群晖反向代理 → 自有域名 HTTPS
```

PWA 至少考虑：

- `manifest.webmanifest`（中文应用名、图标、`display: standalone`）
- 应用图标（含 maskable）
- 静态资源缓存策略
- 缓存版本更新策略

---

## 4. 构建与验证

### Stage 0：变更前自检

- 是否符合当前任务范围。
- 是否需要先读主方案、结构说明、题库规范或部署指南。
- 是否会改变 MVP 边界、技术栈、内容来源或部署方式。
- 是否清楚区分**主方案 / 操作指南 / 原始资料**的职责。
- 是否在用户未确认时创建了代码工程。
- 是否触碰了 §2.1 的八条锁定决策。

### Stage 1：文档验证

文档型改动至少验证：

- 新增或修改文件可读取、可渲染。
- Markdown 标题层级合理，中文 UTF-8 无乱码。
- 仓库内相对链接路径真实存在。
- README / 主方案 / 结构说明 / 题库规范之间**口径一致**（尤其题量与配比）。
- **强制保留段落未被删除或改写**（例如本文件 §5）。
- 主方案改动已同步 `CHANGELOG.md`。

### Stage 2：代码阶段静态检查

代码工程创建后，优先执行受影响范围的检查：

```bash
npm run lint
npm run test
npm run validate:content   # 题库 Schema 与语法白名单校验
npm run build
```

如果尚未创建 `package.json`，应在交付中**明确说明无法运行代码级验证**，不要伪造结论。

### Stage 3：移动端与功能验证

涉及前端页面或交互时，至少验证：

- 360px / 390px / 430px 手机宽度无明显布局破坏。
- 页面无异常横向滚动。
- 主按钮可点击（命中区 ≥ 44px）。
- 关卡流程可前进、可返回。
- 本地进度刷新后不丢失；导出/导入存档可用。
- 可视化播放器每一步的变量值与输出正确。
- 拖拽积木在触屏下可拖动、可吸附、可撤销。

### 常用命令

**本机环境备忘（重要）**：本机 Git Bash 的 `PATH` 异常，命令需前置修复：

```bash
export PATH="/usr/bin:/bin:/c/Windows/System32:/c/Windows:$PATH"
```

**当前文档阶段**：

```bash
export PATH="/usr/bin:/bin:/c/Windows/System32:/c/Windows:$PATH"
ls -R docs
git status --short
git diff --stat
```

**后续代码阶段**：

```bash
npm install
npm run dev
npm run lint
npm run test
npm run build
docker compose up -d
```

---

## 5. 交付输出（每次必给）
1. 新增或修改的文件清单
2. 关键改动说明
3. 验证方式与结论
4. 是否影响行为，以及影响面与回滚点
5. 当前改动属于哪类任务；若属于归档后的新增迭代，可补充当前迭代主题
6. 尚未完成的后续项或阻塞项
7. 建议的 `git commit message`，并带前缀，例如 `docs:`、`feat:`、`fix:`、`chore:`，message为中文
